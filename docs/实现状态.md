# 实现与验证状态

更新：2026-09-24。本文件记录正式仓库的实现证据；需求口径以[协作需求](./协作需求.md)为准。以下临时验证仓库与状态目录均位于系统临时目录 `cc-plugin-managed-smoke-20260924`、`cc-plugin-managed-state-smoke-20260924`，没有在用户业务仓库运行 Claude。

| 能力 | 状态 | 证据与限制 |
| --- | --- | --- |
| 新任务独立 Git 工作树 | 已实现并验证 | 任务 `49aeab75…` 从临时仓库创建独立分支与目录；`delegate_create` 对应 `managed-service.mjs`。 |
| 原生可见窗口、断开重连 | 已实现，部分验证 | `managed-broker.mjs`、`managed-terminal.mjs`、`managed-window.mjs`；正式试跑中窗口客户端连接为 `visible:true`，取消后客户端和桥接进程均退出。临时任务 `f857bc57…` 的终端客户端收到 Ctrl+D 后以 0 退出，后台桥接进程仍在。Windows Terminal 标签页是否按用户当前终端设置自动关闭，尚未做目视确认。 |
| 当前 CCswitch 配置与自定义模型 | 已实现，部分验证 | 实际试跑使用当前 `glm-5.3-flash[1M]`；`managed-state.mjs` 限制模型只能选当前配置。显式选择其他已配置槽尚未做实机调用。 |
| 精确 ID 既有会话续接 | 已实现并验证 | `068d04f8…` 在原临时目录续接；追加指令得到排队、写入、提交、完成事件与“续接完成。”回复。原进程须先退出，外部活动窗口不能直接注入。 |
| 进度事件与正文读取 | 已实现并验证 | `delegate_wait` 读取关键事件，`delegate_transcript` 读取 Claude 会话 JSONL；指令回执与正文在正式试跑中吻合。Codex 任务结束后的主动唤醒仍需宿主调度支持。 |
| 权限门禁与 Codex 批准 | 已实现并验证批准/拒绝/人工兜底 | `node --version` 被归入 Codex 待决；决定 ID `5f8af991…` 批准后工具执行并回复 `v24.12.0`；任务 `10a2171f…` 实测 Codex 拒绝后 Claude 报告拒绝且未重试。任务 `a3bf8ddd…` 将等待缩至 1.5 秒模拟 Codex 失联，终端实际显示 `Permission rule * requires confirmation` 与 `Do you want to proceed?`，没有 Bash 执行事件。凭据和生产部署的真实操作仍需补测。 |
| 顶层并发与排队 | 已实现并验证 | 临时主控任务把上限设为 1；首任务取消后第二任务无需再次调用列表即启动。默认 3、最高 10 的配置范围由服务校验，未做 10 路压力验证。 |
| 主会话轮数与时限 | 已实现，未完整验证 | `managed-broker.mjs` 限 90 分钟、120 主会话轮并保存计数；短审查触发过 2 分钟自定义时限。120 轮未实测。 |
| Claude 子代理限制与门禁继承 | 已实现并验证单个子代理 | 启动时设置 `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`，默认 8、最高 20；正式任务 `2f69294c…` 的子代理 Bash 调用带 `agentId` 进入待决，Codex 批准后才执行。任务 `2d5ce65b…` 验证后台子代理期间任务仍显示运行中，最终回复后才发出指令完成事件。8/20 并发上限尚未压力验证。 |
| 独立只读审查 | 已实现并验证隔离路径 | 任务 `71c31ecc…` 基于实现快照 `3e250ee…` 创建独立审查工作树，与实现目录不同；审查 Claude 核对了 README 改动并提交短报告。定向检查在隔离副本可运行，写入实现文件的工具被拒绝。 |
| 验收合并 | 已实现并验证正常与快照过期路径 | 临时仓库合并提交 `68aedf8…`、`b2b318e…`；第二次通过隔离审查快照验收后合并，目标分支工作树干净。自动化检查验证审查后新增提交会拒绝合并，最新快照合并成功且隔离审查工作树被清理。真实冲突与业务歧义未做实测。 |
| 异常恢复一次 | 已实现并验证中断路径 | 故障注入任务 `7dfd883d…` 验证无指令恢复；任务 `4f1cea1f…` 在 Bash 权限待决时强制终止桥接器和原 Claude，恢复到同一会话 ID，`recoveryInterrupted` 标记原指令未完成。旧权限请求失效已加入自动化检查。 |
| `/交还`、人工输入队列 | 两条人工排队及默认接管已验证 | 正式任务 `b41dd8db…` 中 `/交还` 触发回调；任务 `eaa6d226…` 的两条人工指令先后完成，第一条结束时仍显示忙碌且剩余一条，第二条结束约 5 秒后 Codex 才接管。任务 `752b161d…` 实测方向键和滚动不夺权，输入会切为人工控制。更多终端输入边界仍需验证。 |
| 立即接管 | 已实现并验证单轮中断 | 临时任务 `9c26db77…` 在首条指令提交后收到立即接管，旧指令中断；Claude 的 Escape 未发 Stop 回调，桥接器清理残留草稿后派发下一条，回执 `instruction_submitted`、`instruction_completed` 与“接管后指令。”回复吻合。人工排队同时存在时的立即中断仍需验证。 |
| 准备检查 | 已实现并验证 | `setup` 现在检查当前系统的 Claude 命令、`node-pty` 和已配置模型；默认不消耗模型调用，登录状态明确为未验证。`deep=true` 已在本机通过一次真实 Claude 回复验证，返回当前四个模型槽位与三个实际模型标识。 |

## 检查结果

- `npm test`：当前对外提供的托管桥接器与 MCP 协议检查 20 项通过，覆盖工具列表、旧接口拒绝、准备检查、状态持久化、退出状态对账、模型选择、会话单例、人工输入队列审查闸、权限分类、中断恢复、审查快照与合并、权限回调拒绝及失联兜底。
- 历史 `consult` 测试：此前直接运行全部旧测试时，本机 Windows 结果为 42 项中 21 通过、17 失败、4 跳过；失败主要来自 Unix 假 `claude`、`sleep` 与本地路径导入假设。旧接口已从 MCP 分发中撤下，这些测试仅供研究旧代码，不代表当前托管接口未通过。
- 正式短任务实测：新工作树自动信任（含无可见客户端的定时重绘）、精确会话续接、可见终端连接、队列自动启动、权限待决与批准及拒绝、子代理继承门禁及后台状态、只读审查、正常合并、原进程退出后的自动恢复、`/交还`。
- 安装级检查：独立 `CODEX_HOME` 与本机真实 Codex 配置均已安装 `claude-code@claude-plugin-codex` 0.12.0；从真实已安装缓存启动 MCP，初始化返回 15 个工具，`setup` 检出 Claude Code 2.1.259、当前模型槽和终端依赖；通过 MCP 创建临时任务 `d3162621…`，收到 `terminal_ready` 后取消。当前正在运行的 Codex 桌面任务尚未重新加载插件工具，桌面界面中的调用仍待验证。

## 下一步

1. 补充凭据与生产类操作的安全验证，明确不能无损接入活动中的外部 Claude 窗口这一边界。
2. 在重新加载插件工具的 Codex 桌面任务中调用并观察一次；已安装缓存的 MCP 创建和取消验证通过。
