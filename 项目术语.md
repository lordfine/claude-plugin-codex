# Codex 与 Claude Code 协作术语

本文件统一描述 Codex 向 Claude Code 委派工作时使用的概念，不规定具体实现方式。

## 术语

**主控会话**：由 Codex 承载、负责发起和验收委派任务的会话；它也是执行会话的控制权归属单位。
_避免使用_：纯审查者、只读指挥官。

**执行会话**：由 Claude Code 承载、接受主控会话委派工作的会话。它可以是新会话，也可以是既有会话。
_避免使用_：一次性命令、后台进程。

**既有会话**：用户在接入协作系统之前已创建的 Claude Code 会话。
_避免使用_：可接管窗口。

**委派任务**：主控会话交给执行会话的一项有明确范围的工作，可产生过程反馈、追加指令和最终结果。
_避免使用_：会话。

**接入**：使一个既有会话归入主控会话管理，并建立双向委派关系的过程。
_避免使用_：读取聊天记录、创建全新会话。

**关键进度事件**：需要主控会话知晓或采取行动的执行状态变化，例如求助、失败和完成。
_避免使用_：每一次工具调用、完整日志。

**追加指令**：主控会话在委派任务结束前发给执行会话的新要求。
_避免使用_：立即打断、任务完成后的新委派。

**敏感操作**：可能影响大量文件、凭据、远端系统、生产环境或 Git 历史的执行行为。
_避免使用_：所有工具调用、普通本地编辑。

**权限门禁**：敏感操作执行前取得批准或拒绝决定的协作环节。
_避免使用_：默认自动放行、事后审查。

**当前配置态**：用户为 Claude Code 设置且当前生效的供应商、认证、模型映射及其他设置。
_避免使用_：插件预设模型、Codex 自动切换供应商。

**模型槽位**：当前配置态中由 CCswitch 映射给 Claude Code 的模型选择项，可关联非 Anthropic 名称的模型。
_避免使用_：固定的 Claude 官方模型名单。

**可见执行窗口**：供用户实时观察并直接操作执行会话的 Claude Code 交互窗口。
_避免使用_：静态日志文件、Codex 聊天窗口。

**人工接管**：用户取得执行会话的指令控制权，主控会话暂停自动下达指令的状态。
_避免使用_：Codex 与用户同时自动输入。

**运行上限**：单个委派任务允许的最长运行时间与主会话模型轮次。
_避免使用_：固定预算、账单金额上限。

**指令回执**：与追加指令关联的状态证据，区分已写入、已提交接收、开始处理和完成。
_避免使用_：写入即已处理。

**回执不明**：指令已写入，但尚无足够证据确认执行会话是否接收或处理的状态。
_避免使用_：发送失败、自动重试。

**并发上限**：一个主控会话可同时拥有的顶层执行会话数，不包括执行会话自行开启的子代理。
_避免使用_：固定只能运行一个任务、禁止 Claude 子代理。
